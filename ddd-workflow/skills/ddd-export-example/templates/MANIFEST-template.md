# MANIFEST — <功能名稱> 範例

> 本檔由 ddd-export-example 產生，是這份最小可跑範例的說明書。
> 匯入端（ddd-import-example）會先讀這份檔案再讀程式碼。

## 1. 功能摘要

<一到三句：這個範例示範什麼功能、解決什麼問題。>

- 來源專案：<A 專案名稱 / repo>
- 原始技術棧：<語言 / 框架，例如 Node.js + Express>
- 導出日期：<YYYY-MM-DD>

## 2. 入口

從這裡開始讀：

- `src/<entry>`：<為什麼這是入口>

## 3. 檔案地圖

| 檔案 | 職責 |
|------|------|
| `src/<a>` | <一行說明> |
| `src/<b>` | <一行說明> |
| `<scaffold>` | 最小依賴清單與執行設定 |
| `<smoke test>` | 證明範例能跑的測試 |

## 4. 依賴與執行

- 需要的環境：<例如 Node 18+>
- 安裝依賴：`<安裝指令>`
- 執行 smoke test：`<一行指令>`

外部依賴的處理方式（哪些被換成 stub / fake）：

- <原本是 DB → 換成 in-memory fake，位置在 …>
- <原本是外部 API → 換成假回應>

## 5. 核心概念（語言無關）

> 這是跨棧匯入時的**主要參考**。即使目標專案用不同語言，也能照這段重做。

本質流程：

1. <步驟一：做什麼、為什麼>
2. <步驟二>
3. <步驟三>

關鍵決策 / 容易踩雷的點：

- <例如：token 必須在 X 之前驗證，否則 …>
- <例如：這裡的順序不能換>

## 6. 改寫指引（匯入到別專案時）

| 部分 | 保留 or 替換 | 說明 |
|------|--------------|------|
| <核心邏輯 X> | 保留 | 這是功能本質，照搬 |
| <stub / fake Y> | 替換 | 換成目標專案真實的 DB / 服務 |
| <設定 / 命名 Z> | 替換 | 對齊目標專案的命名與語境 |

## 7. smoke test 摘要

這條測試驗證了：

- **Given** <前提>
- **When** <動作>
- **Then** <預期結果>

> 匯入端會把這條翻譯成目標專案 FXX 的驗收條件，再由 ddd-tdd 跑到綠燈。
