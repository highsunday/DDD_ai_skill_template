interface:
  display_name: "DDD Queue"
  short_description: "Run ordered DDD work items one per fresh agent session."
  default_prompt: "Use ddd-queue to create or execute a DDD queue document for ordered work items."
