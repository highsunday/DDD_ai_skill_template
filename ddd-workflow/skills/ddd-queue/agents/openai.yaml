interface:
  display_name: "DDD Queue"
  short_description: "Run ordered DDD work items and keep an agent communication ledger."
  default_prompt: "Use ddd-queue to create or execute a DDD queue document with an append-only agent communication ledger."
